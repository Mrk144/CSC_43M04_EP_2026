# @package _global_
# Track A: EfficientFormer V2 + temporal transformer (requires timm).
defaults:
  - override /model: efficientformer_transformer

model:
  num_frames: 7

training:
  batch_size: 32