# @package _global_
defaults:
  - override /model: cnn_transformer

model:
  num_frames: 7
  spatial_tokens_side: 1
  num_layers: 4
  num_heads: 8
  mlp_ratio: 2.0
  dropout: 0.1
  drop_path: 0.1

training:
  batch_size: 64
  lr: 3.0e-4
