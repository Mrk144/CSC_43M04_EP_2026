================================================================================
  GUIDE DE LANCEMENT — Train (from scratch / pré-entraîné), test, soumission, ensemble
================================================================================

Répertoire du dépôt : celui qui contient ce fichier (racine projet).
Lancer l'entraînement depuis la racine du dépôt (cwd = racine projet) :

  cd /chemin/vers/CSC_43M04_EP_2026

Environnement :

  cd /chemin/vers/CSC_43M04_EP_2026
  uv sync
  source .venv/bin/activate    # Linux / macOS


--------------------------------------------------------------------------------
0. RAPPELS IMPORTANTS
--------------------------------------------------------------------------------

  • num_classes : 33 (configs/config.yaml).

  • dataset.num_frames : nombre de JPEG lus par clip sur le disque (défaut 4).

  • model.num_frames : longueur temporelle INTERNE au modèle (interpolation
    linéaire dans le forward) — Track A souvent 7, Track B souvent 16 (InternVideo2: 8).

  • Checkpoint sauvé : training.checkpoint_path (défaut "best_model.pt"),
    relatif au répertoire du run Hydra. Après un train simple :
      outputs/YYYY-MM-DD_HH-MM-SS/best_model.pt

  • Pour forcer un dossier de sortie (recommandé) :
      python src/train experiment=... hydra.run.dir=/abs/run/mon_experience

  • Normalisation : si model.pretrained=true, build_transforms utilise les stats
    ImageNet (cohérent VideoMAE, foundation models, ResNet ImageNet).

  • Variable shell utile pour les exemples ci-dessous :
      export CKPT=/abs/chemin/vers/best_model.pt

  • Défauts entraînement (configs/train/default.yaml) :
      lr 5e-4 (fallback) | epochs 40 | warmup 3 | cosine → eta_min 1e-6
      Chaque experiment track_a_* définit son propre peak lr (voir tableau §2).
      Scheduler : warmup linéaire puis cosine (eta_min 1e-6, t_max_ratio 1.0).
      Ralentir le cosine :
        training.scheduler.eta_min=3e-5
        training.scheduler.t_max_ratio=2.0


--------------------------------------------------------------------------------
1. DONNÉES (configs/data/default.yaml)
--------------------------------------------------------------------------------

  train_dir   → processed_data/val2/train   (relatif à cwd = racine projet)
  val_dir     → processed_data/val2/val
  test_dir    → processed_data/val2/test
  submission  → processed_data/val2/submission.csv

Surcharge :

  python src/train dataset.train_dir=/abs/train dataset.val_dir=/abs/val

Tests rapides (sous-échantillon) :

  python src/train experiment=track_a_best dataset.max_samples=500


--------------------------------------------------------------------------------
2. TABLE — ENTRAÎNEMENT FROM SCRATCH (Track A)
--------------------------------------------------------------------------------

Tous ces expériences utilisent model.pretrained=false (sauf si le YAML du modèle
contredit — équilibre du fichier experiment).

  experiment                  | Modèle (group)     | model.num_frames | batch | lr      | epochs | warmup | notes
  -----------------------------+-------------------+----------------+-------+---------+--------+--------+------
  track_a_best                 | tsm_resnet        | 7              | 64    | 1e-3    | 40     | 3      | peak lr (cosine) — référence
  track_a_tsm_resnet34         | tsm_resnet34      | 7              | 64    | 5e-4    | 40     | 3      |
  track_a_tsm_resnet50         | tsm_resnet50      | 7              | 32    | 4e-4    | 40     | 3      |
  track_a_two_stream_gated     | tsm_two_stream_gated | 7           | 24    | 3e-4    | 40     | 3      | RGB + diff, gate R34
  track_a_two_stream_gated_r50 | tsm_two_stream_gated | 7         | 12    | 2.5e-4  | 40     | 3      | backbone ResNet50
  track_a_cnn_lstm_improved    | cnn_lstm_improved | 7              | 64    | 5e-4    | 40     | 3      | ResNet18 + BiLSTM
  track_a_cnn_transformer      | cnn_transformer   | 7              | 64    | 3e-4    | 40     | 3      | ResNet18 + transformer
  track_a_efficientformer_bilstm | efficientformer_bilstm | 7      | 48    | 3e-4    | 40     | 3      | timm + BiLSTM (~16 GB)
  track_a_efficientformer_transformer | efficientformer_transformer | 7 | 32 | 3e-4 | 40 | 3 | timm + transformer (~16 GB)

Commandes (depuis la racine du repo) :

  python src/train experiment=track_a_best
  python src/train experiment=track_a_tsm_resnet34
  python src/train experiment=track_a_tsm_resnet50
  python src/train experiment=track_a_two_stream_gated
  python src/train experiment=track_a_two_stream_gated_r50
  python src/train experiment=track_a_cnn_lstm_improved
  python src/train experiment=track_a_cnn_transformer
  python src/train experiment=track_a_efficientformer_bilstm
  python src/train experiment=track_a_efficientformer_transformer

Tout Track A d'un coup (depuis la racine du repo) :

  ./scripts/train_all_track_a.sh
  WANDB_ARGS="+wandb=on" ./scripts/train_all_track_a.sh
  ONLY="track_a_best track_a_two_stream_gated" ./scripts/train_all_track_a.sh

Une seule ligne (séquentiel avec &&, du plus intéressant au plus léger) :

  python src/train experiment=track_a_two_stream_gated hydra.run.dir=outputs/track_a_two_stream_gated_r34 && python src/train experiment=track_a_efficientformer_transformer hydra.run.dir=outputs/track_a_efficientformer_transformer && python src/train experiment=track_a_best hydra.run.dir=outputs/track_a_tsm_resnet18 && python src/train experiment=track_a_tsm_resnet50 hydra.run.dir=outputs/track_a_tsm_resnet50 && python src/train experiment=track_a_cnn_transformer hydra.run.dir=outputs/track_a_cnn_transformer && python src/train experiment=track_a_efficientformer_bilstm hydra.run.dir=outputs/track_a_efficientformer_bilstm && python src/train experiment=track_a_two_stream_gated_r50 hydra.run.dir=outputs/track_a_two_stream_gated_r50 && python src/train experiment=track_a_tsm_resnet34 hydra.run.dir=outputs/track_a_tsm_resnet34 && python src/train experiment=track_a_cnn_lstm_improved hydra.run.dir=outputs/track_a_cnn_lstm_improved

  # Variante W&B : ajouter +wandb=on après chaque src/train (ou utiliser le script).


VRAM (ordre de grandeur) : two-stream et cnn_transformer plus lourds que
tsm_resnet seul ; réduisez batch_size si OOM.


--------------------------------------------------------------------------------
3. TABLE — ENTRAÎNEMENT PRÉ-ENTRAÎNÉ (Track B)
--------------------------------------------------------------------------------

3.1 Deux phases (geler backbone → finetune complet)

Depuis la racine du dépôt (phase 1 puis phase 2 avec reprise du .pt) :

  # Exemple VideoMAE
  python src/train experiment=track_b_videomae_head
  python src/train experiment=track_b_videomae_finetune \
    training.resume_from=outputs/<run_phase1>/best_model.pt

  # V-JEPA2
  python src/train experiment=track_b_vjepa2_head
  python src/train experiment=track_b_vjepa2_finetune \
    training.resume_from=outputs/<run_phase1>/best_model.pt

  # InternVideo2
  python src/train experiment=track_b_internvideo2_head
  python src/train experiment=track_b_internvideo2_finetune \
    training.resume_from=outputs/<run_phase1>/best_model.pt


TABLE récapitulative phase 1 (head) / phase 2 (finetune) :

  tag            | phase 1 experiment              | bs  | lr    | ep | phase 2 experiment               | bs | lr    | ep
  ---------------+---------------------------------+-----+-------+----+----------------------------------+----+-------+----
  videomae       | track_b_videomae_head           | 8   | 1e-3  | 30 | track_b_videomae_finetune        | 8  | 2e-5  | 30
  vjepa2         | track_b_vjepa2_head             | 8   | 1e-3  | 30 | track_b_vjepa2_finetune          | 4  | 1e-5  | 30
  internvideo2   | track_b_internvideo2_head       | 8   | 1e-3  | 30 | track_b_internvideo2_finetune    | 4  | 1e-5  | 30

Phase 2 nécessite le checkpoint phase 1 : le script place resume_from automatiquement.
Manuel phase 2 :

  python src/train experiment=track_b_videomae_finetune \
    training.resume_from=/abs/phase1/best_model.pt \
    hydra.run.dir=/abs/run/videomae_phase2

Tous les yaml finetune ont training.resume_model_only: true (recharge uniquement
les poids du réseau, pas l’optimiseur de la phase 1).


3.4 Lancer une phase seule (sans script)

  python src/train experiment=track_b_videomae_head hydra.run.dir=/abs/run/vm_head
  python src/train experiment=track_b_vjepa2_finetune \
    training.resume_from=/abs/run/vm_head/best_model.pt \
    hydra.run.dir=/abs/run/vjepa2_ft

Variantes / pistes utiles :

  # VideoMAE autre checkpoint HF
  python src/train experiment=track_b_videomae_head \
    model.variant=MCG-NJU/videomae-base-finetuned-kinetics

  # V-JEPA2 / InternVideo2 : input_size dans configs/model/
  python src/train experiment=track_b_vjepa2_head model.input_size=256


--------------------------------------------------------------------------------
4. ESSAYER UN MODÈLE — ÉVALUATION (validation complète)
--------------------------------------------------------------------------------

Toujours depuis src/. Le checkpoint doit être le best_model.pt du bon run.

  python evaluate.py training.checkpoint_path=$CKPT

Paramètres souvent utiles :

  python evaluate.py \
    training.checkpoint_path=$CKPT \
    training.batch_size=16 \
    training.device=cuda \
    dataset.val_dir=/abs/val

Les métriques affichées (top-1, top-5) utilisent le même prétraitement que
l’entraînement : le flag pretrained du checkpoint pilote ImageNet norm vs [0.5].


--------------------------------------------------------------------------------
5. ESSAYER UN MODÈLE — SOUMISSION (fichier test)
--------------------------------------------------------------------------------

  python create_submission.py training.checkpoint_path=$CKPT

Sortie par défaut : processed_data/val2/submission.csv (sous src/).

  python create_submission.py \
    training.checkpoint_path=$CKPT \
    dataset.submission_output=/abs/soumission.csv \
    dataset.test_manifest=/abs/manifest.csv \
    training.batch_size=8


--------------------------------------------------------------------------------
6. PRÉDICTIONS POUR ENSEMBLE — predict_softmax.py
--------------------------------------------------------------------------------

Par modèle entraîné, génère val.npz + test.npz (alignement des noms de vidéos
obligatoire pour ensemble.py).

  python predict_softmax.py training.checkpoint_path=$CKPT

Dossier de sortie explicite (recommandé si plusieurs modèles) :

  python predict_softmax.py \
    training.checkpoint_path=$CKPT \
    predict.output_dir=/abs/preds/nom_du_modele

Contenu : val.npz (softmax, labels, val_accuracy), test.npz (softmax).


--------------------------------------------------------------------------------
7. ENSEMBLE — soft voting (pas un MoE entraîné)
--------------------------------------------------------------------------------

  1) predict_softmax pour chaque checkpoint
  2) Éditer src/configs/ensemble/default.yaml : val_npz, test_npz par modèle
  3) Lancer :

  python ensemble.py +ensemble=default

Méthodes :

  python ensemble.py +ensemble=default ensemble.method=mean_softmax
  python ensemble.py +ensemble=default ensemble.method=weighted_softmax ensemble.weights=[0.6,0.4]
  python ensemble.py +ensemble=default ensemble.method=weighted_softmax ensemble.weights=auto
  python ensemble.py +ensemble=default ensemble.method=temperature_then_mean \
    ensemble.output_csv=/abs/submission_ensemble.csv


--------------------------------------------------------------------------------
8. VRAM (~20 Go) — PARAMÈTRES À AJUSTER
--------------------------------------------------------------------------------

  • Réduire training.batch_size (foundation models : 8 → 4 → 2 → 1).
  • track_a_best batch 64 → 32 ou 16
  • model.variant Kinetics au lieu de SSv2 si besoin (moins adapté à la tâche)
  • dataset.max_samples pour valider le pipeline sans saturer la mémoire


--------------------------------------------------------------------------------
9. RÉFÉRENCE RAPIDE DES OVERRIDE HYDRA
--------------------------------------------------------------------------------

  training.checkpoint_path
  training.device             cuda | cpu
  training.batch_size
  training.epochs
  training.lr
  training.warmup_epochs
  training.resume_from
  training.resume_model_only
  training.num_workers
  training.optimizer.name     adamw | sgd | adam
  training.optimizer.weight_decay
  training.loss.name          focal_loss (defaut) | cross_entropy
  training.loss.label_smoothing
  model.name / model.pretrained / model.num_frames / model.backbone / model.variant
  model.freeze_backbone
  dataset.num_frames / dataset.max_samples
  hydra.run.dir


--------------------------------------------------------------------------------
10. ENCHAÎNEMENT TYPE COMPÉTITION
--------------------------------------------------------------------------------

  A) Entraîner (from scratch Track A et/ou pré-entraîné Track B), noter chaque
     .../best_model.pt
  B) evaluate.py sur chaque .pt
  C) predict_softmax.py par .pt → dossiers preds distincts
  D) Renseigner configs/ensemble/default.yaml puis ensemble.py
  E) Soumettre le CSV final (meilleur solo ou ensemble)


================================================================================
  Fin du guide — remplacer $CKPT et les chemins /abs par tes chemins réels.
================================================================================