# @package _global_
# Track A: EfficientFormer V2 + BiLSTM + attention (requires timm).
defaults:
  - override /model: efficientformer_bilstm

model:
  num_frames: 7

training:
  batch_size: 48
  lr: 3.0e-4