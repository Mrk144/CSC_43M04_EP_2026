# @package _global_
defaults:
  - override /model: videomae

model:
  freeze_backbone: true
  num_frames: 16

training:
  batch_size: 8
  lr: 1e-3

  epochs: 30
  warmup_epochs: 1
  num_workers: 12

  optimizer:
    name: adamw
    weight_decay: 0.05
