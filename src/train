#!/usr/bin/env python3
"""Launcher for train.py — use: python src/train (from repo root)."""
import runpy
import sys
from pathlib import Path

_SRC = Path(__file__).resolve().parent
sys.path.insert(0, str(_SRC))
runpy.run_path(str(_SRC / "train.py"), run_name="__main__")
