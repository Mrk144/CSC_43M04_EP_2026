# @package _global_
# Track B (open world): fine-tune a Kinetics-pretrained video backbone.
# Lower LR than from-scratch because the backbone is already well-trained.
# The backbone internally upsamples 4 raw frames -> 16 (matches K400 setup).
defaults:
  - override /model: pretrained_video

model:
  num_frames: 16

training:
  batch_size: 16
  lr: 1e-4
  epochs: 20
  warmup_epochs: 2
  num_workers: 12

  optimizer:
    name: adamw
    weight_decay: 0.05

  loss:
    name: cross_entropy
    label_smoothing: 0.1
