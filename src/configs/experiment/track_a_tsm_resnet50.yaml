# @package _global_
# Track A: TSM + ResNet50 (single stream).
defaults:
  - override /model: tsm_resnet50

model:
  num_frames: 7

training:
  batch_size: 32
  lr: 3e-4
  epochs: 40
  warmup_epochs: 3
  num_workers: 12

  optimizer:
    name: adamw
    weight_decay: 0.05
