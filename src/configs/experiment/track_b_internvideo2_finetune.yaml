# @package _global_
defaults:
  - override /model: internvideo2

model:
  freeze_backbone: false
  num_frames: 8

training:
  batch_size: 4
  lr: 1e-5

  epochs: 30
  warmup_epochs: 1
  num_workers: 12

  resume_model_only: true

  optimizer:
    name: adamw
    weight_decay: 0.05
