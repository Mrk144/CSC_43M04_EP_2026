# @package _global_
defaults:
  - override /model: videomae

model:
  freeze_backbone: false
  num_frames: 16

training:
  batch_size: 8
  lr: 1e-5

  epochs: 30
  warmup_epochs: 1
  num_workers: 12

  resume_model_only: true

  optimizer:
    name: adamw
    weight_decay: 0.05
