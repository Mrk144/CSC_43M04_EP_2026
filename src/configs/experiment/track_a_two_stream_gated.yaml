# @package _global_
# Track A: two-stream TSM gated fusion, ResNet34 per stream (default).
defaults:
  - override /model: tsm_two_stream_gated

model:
  num_frames: 7
  backbone: resnet34

training:
  batch_size: 32
  lr: 3.0e-4